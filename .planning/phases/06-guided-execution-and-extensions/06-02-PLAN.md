---
phase: 06-guided-execution-and-extensions
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - Sources/ConjectureParallel/ParallelRunner.swift
  - Sources/ConjectureParallel/ParallelConfig.swift
  - Tests/ConjectureParallelTests/ParallelDeterminismTests.swift
autonomous: true
requirements: [PARA-01]

must_haves:
  truths:
    - "ParallelRunner distributes runs across a task group with deterministic seed-per-index mapping"
    - "Completion order normalization ensures reproducible failure detection regardless of scheduling"
    - "Parallel execution preserves the same seed-to-value mapping as sequential Runner"
    - "ConjectureParallel compiles under Swift 6 strict concurrency"
  artifacts:
    - path: "Sources/ConjectureParallel/ParallelRunner.swift"
      provides: "Deterministic parallel property runner"
      exports: ["ParallelRunner"]
    - path: "Sources/ConjectureParallel/ParallelConfig.swift"
      provides: "Parallel execution configuration"
      exports: ["ParallelConfig"]
    - path: "Tests/ConjectureParallelTests/ParallelDeterminismTests.swift"
      provides: "Determinism and seed distribution tests"
---

<objective>
Implement ConjectureParallel module with a ParallelRunner that distributes property runs across a Swift task group using deterministic seed-per-index mapping, ensuring reproducible results regardless of completion order.

Purpose: Deliver PARA-01 -- users can run properties in parallel with deterministic seed distribution.
Output: Working ConjectureParallel module with ParallelRunner and tests proving determinism.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement ParallelRunner with deterministic seed distribution</name>
  <files>
    - Sources/ConjectureParallel/ParallelConfig.swift
    - Sources/ConjectureParallel/ParallelRunner.swift
  </files>
  <action>
Create ParallelConfig with maxConcurrentRuns and ParallelRunner that uses
withThrowingTaskGroup to fan out runs with deterministic seed-per-index mapping,
then normalizes results by logical index before returning.
  </action>
  <done>ParallelRunner compiles under Swift 6 strict concurrency and uses task group with index-keyed seed derivation.</done>
</task>

<task type="auto">
  <name>Task 2: Add determinism and parallel execution tests</name>
  <files>
    - Tests/ConjectureParallelTests/ParallelDeterminismTests.swift
  </files>
  <action>
Write tests proving: same seed produces same results in parallel and sequential mode,
completion order does not affect outcome, first failure is returned deterministically.
  </action>
  <done>All ParallelDeterminismTests pass with zero warnings.</done>
</task>

</tasks>

<verification>
1. `swift build --explicit-target-dependency-import-check error -Xswiftc -warnings-as-errors` passes
2. `swift test --filter ParallelDeterminismTests` passes
</verification>

<success_criteria>
- ParallelRunner uses task group with deterministic seed-per-index mapping
- Results are normalized by logical index, not completion order
- All tests pass under strict concurrency
- No back-edges into core types
</success_criteria>
