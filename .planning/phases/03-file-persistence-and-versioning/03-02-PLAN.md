---
phase: 03-file-persistence-and-versioning
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Sources/PremiseDatabase/FileBackedDatabase.swift
  - Tests/PremiseDatabaseTests/FileBackedDatabasePersistenceTests.swift
autonomous: true
requirements:
  - PERS-01
  - PERS-03
user_setup: []
must_haves:
  truths:
    - Failing examples persist locally in a file-backed v1 database.
    - Persisted traces load deterministically by `PropertyIdentity` for replay use.
    - Clearing a property removes its stored replay artifacts.
  artifacts:
    - Sources/PremiseDatabase/FileBackedDatabase.swift
    - Tests/PremiseDatabaseTests/FileBackedDatabasePersistenceTests.swift
  key_links:
    - `FileBackedDatabase.save` must write versioned records encoded by `PersistenceCodec`.
    - `FileBackedDatabase.loadTraces` must decode persisted records and return traces in stored order.
---

<objective>
Implement the v1 file-backed failure store.

Purpose: Deliver concrete local persistence semantics while keeping the storage contract compatible with future backends.
Output: Real file I/O implementation of `FileBackedDatabase` and persistence lifecycle tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-file-persistence-and-versioning/03-RESEARCH.md
@.planning/phases/03-file-persistence-and-versioning/03-VALIDATION.md
@.planning/phases/03-file-persistence-and-versioning/03-01-PLAN.md
@Sources/PremiseDatabase/ExampleDatabase.swift
@Sources/PremiseDatabase/FileBackedDatabase.swift
@Sources/PremiseDatabase/PersistenceFormats.swift
@Sources/PremiseDatabase/PersistenceCodec.swift

<interfaces>
From `Sources/PremiseDatabase/ExampleDatabase.swift`:
```swift
public protocol ExampleDatabase: Actor {
  func loadTraces(for id: PropertyIdentity) async throws -> [ChoiceTrace]
  func save(_ record: FailureRecord) async throws
  func clear(for id: PropertyIdentity) async throws
}
```

From `Sources/PremiseDatabase/PersistenceCodec.swift` (from Plan 03-01):
```swift
func encode(_ record: FailureRecord) -> PersistedFailureRecordV1
func decode(_ envelope: PersistedFailureRecordV1) throws -> FailureRecord
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder FileBackedDatabase with atomic local persistence</name>
  <files>Sources/PremiseDatabase/FileBackedDatabase.swift</files>
  <read_first>Sources/PremiseDatabase/FileBackedDatabase.swift, Sources/PremiseDatabase/ExampleDatabase.swift, Sources/PremiseDatabase/PersistenceFormats.swift, Sources/PremiseDatabase/PersistenceCompatibility.swift, Sources/PremiseDatabase/PersistenceCodec.swift, Sources/PremiseCore/PropertyIdentity.swift, .planning/phases/03-file-persistence-and-versioning/03-RESEARCH.md, AGENTS.md, RULES.md</read_first>
  <action>Per PERS-01 and PERS-03, replace the placeholder actor implementation with real file storage. Use a deterministic root path default of `./.premise/examples` and ensure the directory is created with intermediates. Persist one JSON file per property identity, where file names are the hex-encoded UTF-8 bytes of `"\(fileID)#\(line)#\(strategyLabel)"` plus `.json`. On `save`, read existing property records (if file exists), call `PersistenceCodec.encode(_:)` for the incoming `FailureRecord`, append the encoded `PersistedFailureRecordV1`, and write atomically (`Data.write(..., options: [.atomic])`). On `loadTraces`, decode each persisted envelope via `PersistenceCodec.decode(_:)` and return `[ChoiceTrace]` in persisted order. On `clear`, remove only the targeted property file and treat missing files as a no-op.</action>
  <verify>
    <automated>swift build --target PremiseDatabase --explicit-target-dependency-import-check error -Xswiftc -warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `Sources/PremiseDatabase/FileBackedDatabase.swift` contains `public actor FileBackedDatabase`
    - `Sources/PremiseDatabase/FileBackedDatabase.swift` contains `.premise/examples`
    - `Sources/PremiseDatabase/FileBackedDatabase.swift` contains `options: [.atomic]`
    - `Sources/PremiseDatabase/FileBackedDatabase.swift` contains `loadTraces`
    - `Sources/PremiseDatabase/FileBackedDatabase.swift` contains `save`
    - `Sources/PremiseDatabase/FileBackedDatabase.swift` contains `clear`
    - `swift build --target PremiseDatabase --explicit-target-dependency-import-check error -Xswiftc -warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The file-backed actor persists and retrieves property-scoped failure records locally using the versioned format contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add persistence lifecycle tests for save/load/clear behavior</name>
  <files>Tests/PremiseDatabaseTests/FileBackedDatabasePersistenceTests.swift</files>
  <read_first>Tests/PremiseDatabaseTests/FileBackedDatabasePersistenceTests.swift, Sources/PremiseDatabase/FileBackedDatabase.swift, Sources/PremiseDatabase/PersistenceFormats.swift, Sources/PremiseCore/PremiseCore.swift, .planning/phases/03-file-persistence-and-versioning/03-VALIDATION.md, AGENTS.md, RULES.md</read_first>
  <action>Create `FileBackedDatabasePersistenceTests.swift` with tests that use a temporary directory per test: (1) saving two records for the same property returns two traces from `loadTraces` in insertion order; (2) `clear(for:)` removes traces for that property and leaves other properties untouched; (3) loading a property with no file returns an empty array.</action>
  <verify>
    <automated>swift test --filter FileBackedDatabasePersistenceTests</automated>
  </verify>
  <acceptance_criteria>
    - `Tests/PremiseDatabaseTests/FileBackedDatabasePersistenceTests.swift` contains `@Test`
    - `Tests/PremiseDatabaseTests/FileBackedDatabasePersistenceTests.swift` contains `loadTraces`
    - `Tests/PremiseDatabaseTests/FileBackedDatabasePersistenceTests.swift` contains `clear(for:`
    - `swift test --filter FileBackedDatabasePersistenceTests` exits 0
  </acceptance_criteria>
  <done>Automated tests prove local persistence, retrieval ordering, and property-scoped clearing behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build --target PremiseDatabase --explicit-target-dependency-import-check error -Xswiftc -warnings-as-errors`
- [ ] `swift test --filter FileBackedDatabasePersistenceTests`
</verification>

<success_criteria>

- PERS-01 local persistence behavior is implemented and test-verified
- Persisted records are written/read via the versioned format contract from 03-01
- `FileBackedDatabase` no longer contains placeholder no-op logic

</success_criteria>

<output>
After completion, create `.planning/phases/03-file-persistence-and-versioning/03-02-SUMMARY.md`
</output>
